export enum FilterType {
  Boolean = 'boolean',
  BooleanCheckbox = 'boolean-checkbox',
  BooleanDropdown = 'boolean-dropdown',
  Custom = 'custom',
  Date = 'date',
  Dropdown = 'dropdown',
  Text = 'text',
}
